[build]
  command = "npm install"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

# functions yolu varsayılan: netlify/functions — [build] içinde "functions" kullanılmamalı (parse hatası)
[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true

[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200
