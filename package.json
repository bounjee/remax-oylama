{
  "name": "remax-best-full-system",
  "version": "1.0.1",
  "description": "RE/MAX Best siklik oylamasi - duzeltilmis stil",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:netlify": "npx --yes netlify-cli dev",
    "netlify:login": "npx --yes netlify-cli login",
    "netlify:link": "npx --yes netlify-cli link",
    "netlify:deploy": "npx --yes netlify-cli deploy --build",
    "netlify:deploy:prod": "npx --yes netlify-cli deploy --build --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.2",
    "cookie-session": "^2.1.0",
    "express": "^4.21.2",
    "serverless-http": "^3.2.0"
  }
}